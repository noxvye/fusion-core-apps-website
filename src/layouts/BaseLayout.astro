---
import "@/styles/global.css";

import GooglePlayLogo from "@/assets/gplay-logo.svg";
import { LanguagePicker } from "@/components/LanguagePicker.tsx";
import ThemeToggle from "@/components/ThemeToggle.tsx";
import { getLocalePath, getLocaleFromPath, stripLocalePath, useTranslations, type Locale } from "@/i18n/utils";

const {
	title = "FusionCore Apps — Premium Mobile Applications",
	description = "Professional mobile app development studio delivering secure, innovative, and user-friendly applications for Android and iOS platforms. Transform your ideas into powerful digital experiences.",
	ogImage = "/og-default.png",
	// Actual pixel dimensions of ogImage (og-default.png and blog images are 1536×1024;
	// pages using the 512×512 app icon must pass 512/512).
	ogImageWidth = 1536,
	ogImageHeight = 1024,
	ogType = "website",
	canonical,
	noindex = false,
	bodyClass = "",
	showHero = false,
	locale = "en",
	showFooterCta = true,
	// Locales for which a real translation of the current page exists.
	// Defaults to all three for backward compat (homepage, /apps, /about, etc.).
	// Pages like blog posts SHOULD pass an explicit list so Google does not infer
	// translations that do not exist (causes 404s in GSC — e.g., /pt/blog/<en-only-slug>).
	availableLocales = ["en", "es", "pt"] as Array<"en" | "es" | "pt">,
	// Optional override for the per-locale URLs of the current page.
	// Default behavior derives sibling URLs by naive path translation
	// (e.g., /blog/foo → /es/blog/foo), which is wrong when translated
	// posts live under different slugs. Pages with a `translationKey`
	// should pass exact URLs here so hreflang and the language switcher
	// route to the correct locale-specific slugs.
	localeLinks: localeLinksOverride = undefined as
		| Partial<{ en: string; es: string; pt: string }>
		| undefined,
} = Astro.props;

const year = new Date().getFullYear();
const currentPath = Astro.url.pathname;

// i18n — compute locale links from current pathname
const currentLocale = locale ?? getLocaleFromPath(currentPath);
const basePath = stripLocalePath(currentPath);
const defaultLocaleLinks = {
	en: getLocalePath(basePath, "en"),
	es: getLocalePath(basePath, "es"),
	pt: getLocalePath(basePath, "pt"),
};
// Partial-merge so callers can override only the locales they have real
// translations for; the rest fall back to the naive path translation
// (used by the language switcher when no override is supplied).
const localeLinks = { ...defaultLocaleLinks, ...(localeLinksOverride ?? {}) };
const t = useTranslations(currentLocale as Locale);

// Locale-aware nav hrefs
const navHome    = getLocalePath("/", currentLocale as Locale);
const navApps    = getLocalePath("/apps", currentLocale as Locale);
const navBlog    = getLocalePath("/blog", currentLocale as Locale);
const navPrivacy = getLocalePath("/privacy", currentLocale as Locale);
const navContact = getLocalePath("/contact", currentLocale as Locale);
const navTerms   = getLocalePath("/terms", currentLocale as Locale);
// IMPORTANT: Use Astro.url.pathname (not Astro.url) — pathname strips query params.
// Using Astro.url.href would bake ?utm_source=... or ?from=AppAgg.com into the canonical,
// creating duplicate-content variants that confuse Googlebot. Never change this to Astro.url.
const canonicalUrl = canonical ?? new URL(Astro.url.pathname, Astro.site).href;

// Handle ogImage: if it's already a full URL or data URI, use as-is; otherwise make it absolute
const resolvedOgImage =
	ogImage.startsWith("http://") || ogImage.startsWith("https://") || ogImage.startsWith("data:")
		? ogImage
		: new URL(ogImage, Astro.site).href;
---

<!doctype html>
<html class="scroll-smooth" lang={locale}>
	<head>
		<meta charset="utf-8" />
		<meta content="width=device-width, initial-scale=1, viewport-fit=cover" name="viewport" />
		<title>{title}</title>
		<meta content={description} name="description" />
		<meta content={noindex ? "noindex,nofollow" : "index,follow"} name="robots" />
		<link href={canonicalUrl} rel="canonical" />
		{availableLocales.includes("en") && (
			<link href={new URL(localeLinks.en, Astro.site).href} hreflang="x-default" rel="alternate" />
		)}
		{availableLocales.includes("en") && (
			<link href={new URL(localeLinks.en, Astro.site).href} hreflang="en" rel="alternate" />
		)}
		{availableLocales.includes("es") && (
			<link href={new URL(localeLinks.es, Astro.site).href} hreflang="es-419" rel="alternate" />
		)}
		{availableLocales.includes("pt") && (
			<link href={new URL(localeLinks.pt, Astro.site).href} hreflang="pt-BR" rel="alternate" />
		)}

		<meta content={ogType} property="og:type" />
		<meta content={new URL(Astro.url.pathname, Astro.site).href} property="og:url" />
		<meta content={title} property="og:title" />
		<meta content={description} property="og:description" />
		<meta content={resolvedOgImage} property="og:image" />
		<meta content={String(ogImageWidth)} property="og:image:width" />
		<meta content={String(ogImageHeight)} property="og:image:height" />
		<meta content="FusionCore Apps" property="og:site_name" />
		<meta content={currentLocale === "es" ? "es_MX" : currentLocale === "pt" ? "pt_BR" : "en_US"} property="og:locale" />

		<meta content="summary_large_image" name="twitter:card" />
		<meta content="@FusioncoreApps" name="twitter:site" />
		<meta content={new URL(Astro.url.pathname, Astro.site).href} name="twitter:url" />
		<meta content={title} name="twitter:title" />
		<meta content={description} name="twitter:description" />
		<meta content={resolvedOgImage} name="twitter:image" />

		<meta content="#2ECFFF" name="theme-color" media="(prefers-color-scheme: light)" />
		<meta content="#0b1c2d" name="theme-color" media="(prefers-color-scheme: dark)" />
		<meta content="light dark" name="color-scheme" />
		<meta content="yes" name="mobile-web-app-capable" />
		<meta content="default" name="apple-mobile-web-app-status-bar-style" />

		<link rel="preconnect" href="https://www.gstatic.com" crossorigin />
		<link rel="preload" href="/fonts/bricolage-grotesque-variable.woff2" as="font" type="font/woff2" crossorigin />
		<link rel="preload" href="/fonts/inter-variable.woff2" as="font" type="font/woff2" crossorigin />

		<link href="/favicon.svg" rel="icon" type="image/svg+xml" />
		<!-- apple-touch-icon.png must be a 180×180 PNG at /public/apple-touch-icon.png -->
		<link href="/apple-touch-icon.png" rel="apple-touch-icon" sizes="180x180" />

		<script is:inline type="application/ld+json">
			{
				"@context": "https://schema.org",
				"@type": "Organization",
				"name": "FusionCore Apps",
				"description": "Professional mobile app development studio",
				"url": "https://www.fusioncoreapps.com",
				"logo": {
					"@type": "ImageObject",
					"url": "https://www.fusioncoreapps.com/logo.png",
					"width": 528,
					"height": 551
				},
				"foundingDate": "2024",
				"sameAs": ["https://play.google.com/store/apps/dev?id=5173536626513659112"]
			}
		</script>
		<script type="application/ld+json" set:html={JSON.stringify({
			"@context": "https://schema.org",
			"@type": "WebSite",
			"url": "https://www.fusioncoreapps.com",
			"name": "FusionCore Apps",
			"potentialAction": {
				"@type": "SearchAction",
				"target": `https://www.fusioncoreapps.com/${currentLocale !== "en" ? currentLocale + "/" : ""}apps?q={search_term_string}`,
				"query-input": "required name=search_term_string"
			}
		}).replace(/</g, "\\u003c")}></script>

		<script is:inline>
			(function () {
				const stored = localStorage.getItem("theme");
				const prefersDark = window.matchMedia("(prefers-color-scheme: dark)").matches;
				const isDark = stored === "dark" || (!stored && prefersDark);

				if (isDark) {
					document.documentElement.classList.add("dark");
				}
			})();
		</script>
		<slot name="head" />

		<!-- Firebase Analytics -->
		{import.meta.env.PUBLIC_FIREBASE_MEASUREMENT_ID && (
			<script type="module" define:vars={{
				apiKey: import.meta.env.PUBLIC_FIREBASE_API_KEY,
				authDomain: import.meta.env.PUBLIC_FIREBASE_AUTH_DOMAIN,
				projectId: import.meta.env.PUBLIC_FIREBASE_PROJECT_ID,
				storageBucket: import.meta.env.PUBLIC_FIREBASE_STORAGE_BUCKET,
				messagingSenderId: import.meta.env.PUBLIC_FIREBASE_MESSAGING_SENDER_ID,
				appId: import.meta.env.PUBLIC_FIREBASE_APP_ID,
				measurementId: import.meta.env.PUBLIC_FIREBASE_MEASUREMENT_ID,
			}}>
				import { initializeApp } from "https://www.gstatic.com/firebasejs/11.6.0/firebase-app.js";
				import { getAnalytics } from "https://www.gstatic.com/firebasejs/11.6.0/firebase-analytics.js";
				const app = initializeApp({ apiKey, authDomain, projectId, storageBucket, messagingSenderId, appId, measurementId });
				getAnalytics(app);
			</script>
		)}
	</head><body class:list={[`min-h-screen antialiased`, `${bodyClass}`]}>
		<!-- Skip to content link for accessibility -->
		<a
			class="button button--primary sr-only focus:not-sr-only focus:absolute focus:top-4 focus:left-4 focus:z-50"
			href="#main"
		>
			{t("footer.skipToContent")}
		</a>

		<!-- ── Header: Morphing Glass Pill ── -->
		<header id="site-header" class="header sticky top-0 z-40 w-full">
			<div class="px-4 py-3 sm:px-5">
				<div
					id="header-pill"
					class="header__pill mx-auto flex max-w-7xl items-center justify-between"
				>
					<!-- Logo -->
					<a href={navHome} class="header__logo group flex min-w-0 flex-shrink-0 items-center gap-2.5">
						<img
							src="/logo.png"
							alt="FusionCore Apps"
							width="36"
							height="36"
							class="h-9 w-9 flex-shrink-0 rounded-xl object-contain transition-transform duration-300 group-hover:scale-105"
						/>
						<span
							class="header__wordmark overflow-hidden whitespace-nowrap font-bold text-gray-900 dark:text-white"
							style="font-family: var(--font-display); letter-spacing: -0.025em; font-size: 15px;"
						>FusionCore<span class="text-brand-700 dark:text-brand-400"> Apps</span></span>
					</a>

					<!-- Desktop links -->
					<ul class="hidden items-center gap-0.5 md:flex" role="menubar">
						<li role="none">
							<a
								class:list={["nav-pill-link", { "nav-pill-link--active": basePath.startsWith("/apps") }]}
								href={navApps}
								role="menuitem"
							>{t("nav.apps")}</a>
						</li>
						<li role="none">
							<a
								class:list={["nav-pill-link", { "nav-pill-link--active": basePath.startsWith("/blog") }]}
								href={navBlog}
								role="menuitem"
							>{t("nav.blog")}</a>
						</li>
						<li role="none">
							<a
								class:list={["nav-pill-link", { "nav-pill-link--active": basePath === "/privacy" }]}
								href={navPrivacy}
								role="menuitem"
							>{t("nav.privacy")}</a>
						</li>
						<li role="none">
							<a
								class:list={["nav-pill-link", { "nav-pill-link--active": basePath === "/contact" }]}
								href={navContact}
								role="menuitem"
							>{t("nav.contact")}</a>
						</li>
					</ul>

					<!-- Actions -->
					<div class="flex flex-shrink-0 items-center gap-2">
						<ThemeToggle client:idle />
						<LanguagePicker currentLocale={currentLocale} links={localeLinks} client:idle />

						<!-- Desktop CTA -->
						<a href={navApps} class="nav-cta hidden items-center gap-1.5 md:inline-flex">
							{t("nav.browseApps")}
							<svg class="h-3.5 w-3.5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
								<path d="M9 18l6-6-6-6"/>
							</svg>
						</a>

						<!-- Animated hamburger for mobile -->
						<button
							id="menu-btn"
							class="menu-btn flex h-8 w-8 flex-shrink-0 items-center justify-center rounded-lg text-gray-500 transition-colors duration-150 hover:bg-gray-100 hover:text-gray-800 focus:outline-none dark:text-gray-400 dark:hover:bg-gray-800 dark:hover:text-gray-100 md:hidden"
							aria-label={t("nav.openMenu")}
							aria-expanded="false"
							aria-controls="mobile-overlay"
						>
							<span class="flex h-5 w-5 flex-col items-center justify-center gap-[5.5px]">
								<span class="menu-btn__bar w-5"></span>
								<span class="menu-btn__bar w-[14px] self-start"></span>
								<span class="menu-btn__bar w-5"></span>
							</span>
						</button>
					</div>
				</div>
			</div>
		</header>

		<!-- ── Mobile Fullscreen Overlay ── -->
		<div
			id="mobile-overlay"
			class="mobile-overlay fixed inset-0 z-50 flex flex-col md:hidden"
			aria-hidden="true"
			role="dialog"
			aria-modal="true"
			aria-label="Navigation menu"
		>
			<!-- Top bar -->
			<div class="flex flex-shrink-0 items-center justify-between px-6 py-4">
				<a href={navHome} class="flex items-center gap-2.5" id="overlay-logo">
					<img src="/logo.png" alt="FusionCore Apps" class="h-8 w-8 flex-shrink-0 rounded-lg object-contain" />
					<span class="font-bold text-gray-900 dark:text-white" style="font-family: var(--font-display); font-size: 15px; letter-spacing: -0.02em;">
						FusionCore<span class="text-brand-700 dark:text-brand-400"> Apps</span>
					</span>
				</a>
				<button
					id="close-overlay"
					class="flex h-8 w-8 items-center justify-center rounded-lg text-gray-500 transition-colors duration-150 hover:bg-gray-100 hover:text-gray-800 dark:text-gray-400 dark:hover:bg-gray-800 dark:hover:text-gray-100"
					aria-label={t("nav.closeMenu")}
				>
					<svg class="h-5 w-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
						<path d="M18 6L6 18M6 6l12 12"/>
					</svg>
				</button>
			</div>

			<!-- Nav links — large editorial type -->
			<div class="flex flex-1 flex-col justify-center px-8 -mt-6">
				<nav aria-label="Mobile navigation links">
					<ul class="divide-y divide-gray-200/70 dark:divide-white/5">
						<li class="mobile-link-item">
							<a
								class:list={["mobile-nav-link", { "mobile-nav-link--active": basePath.startsWith("/apps") }]}
								href={navApps}
								id="mobile-link-apps"
							>
								<span>{t("nav.apps")}</span>
								<svg class="h-6 w-6 flex-shrink-0 opacity-30 transition-opacity duration-200" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 18l6-6-6-6"/></svg>
							</a>
						</li>
						<li class="mobile-link-item">
							<a
								class:list={["mobile-nav-link", { "mobile-nav-link--active": basePath.startsWith("/blog") }]}
								href={navBlog}
								id="mobile-link-blog"
							>
								<span>{t("nav.blog")}</span>
								<svg class="h-6 w-6 flex-shrink-0 opacity-30 transition-opacity duration-200" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 18l6-6-6-6"/></svg>
							</a>
						</li>
						<li class="mobile-link-item">
							<a
								class:list={["mobile-nav-link", { "mobile-nav-link--active": basePath === "/privacy" }]}
								href={navPrivacy}
								id="mobile-link-privacy"
							>
								<span>{t("nav.privacy")}</span>
								<svg class="h-6 w-6 flex-shrink-0 opacity-30 transition-opacity duration-200" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 18l6-6-6-6"/></svg>
							</a>
						</li>
						<li class="mobile-link-item">
							<a
								class:list={["mobile-nav-link", { "mobile-nav-link--active": basePath === "/contact" }]}
								href={navContact}
								id="mobile-link-contact"
							>
								<span>{t("nav.contact")}</span>
								<svg class="h-6 w-6 flex-shrink-0 opacity-30 transition-opacity duration-200" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 18l6-6-6-6"/></svg>
							</a>
						</li>
					</ul>
				</nav>

				<!-- Subtle stat row -->
				<p class="mt-8 text-xs font-medium tracking-widest uppercase text-gray-400 dark:text-gray-600">
					{t("footer.mobileStats")}
				</p>
			</div>

			<!-- Bottom CTA -->
			<div class="mobile-overlay__footer flex-shrink-0 px-8 pb-12 pt-6">
				<a
					href={navApps}
					id="mobile-cta"
					class="flex w-full items-center justify-center gap-2 rounded-2xl py-4 text-base font-bold text-white shadow-sm"
					style="background: var(--color-brand-700);"
				>
					{t("footer.browseAllApps")}
					<svg class="h-4 w-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
						<path d="M9 18l6-6-6-6"/>
					</svg>
				</a>
			</div>
		</div>

		<!-- Main Content -->
		<main class="main flex-1" id="main" role="main">
			{
				showHero && (
					<section class="hero relative overflow-hidden">
						<div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
							<slot name="hero" />
						</div>
					</section>
				)
			}

			<div class:list={{ "py-16 sm:py-24": showHero, "py-8 sm:py-12": !showHero }}>
				<div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
					<slot />
				</div>
			</div>
		</main>

		<!-- ── Footer ── -->
		<footer role="contentinfo">

			<!-- Top hairline -->
			<div class="h-px w-full bg-gray-200 dark:bg-white/10"></div>

			<!-- CTA Island — theme-aware -->
			{showFooterCta && (
				<div class="footer-cta-island">
					<div class="relative mx-auto flex max-w-7xl flex-col items-center gap-6 px-4 py-20 text-center sm:px-6 lg:px-8">
						<div class="footer-cta-island__label section-label">
							<span class="inline-block h-1.5 w-1.5 rounded-full mr-2 opacity-50"></span>
							{t("footer.availableOn")}
						</div>
						<h2 class="footer-cta-island__title">
							{t("footer.ctaTitle")}
						</h2>
						<p class="footer-cta-island__body">
							{t("footer.ctaBody")}
						</p>
						<a href={navApps} class="footer-cta-island__btn">
							<svg class="h-4 w-4 flex-shrink-0" viewBox="0 0 24 24" fill="none">
								<path d="M3 20.5V3.5C3 2.91 3.34 2.39 3.84 2.15L13.69 12L3.84 21.85C3.34 21.6 3 21.09 3 20.5Z" fill="#2ecfff" opacity="0.9"/>
								<path d="M16.81 15.12L6.05 21.34L14.54 12.85L16.81 15.12Z" fill="#ffc107" opacity="0.9"/>
								<path d="M20.16 10.81C20.5 11.08 20.75 11.5 20.75 12C20.75 12.5 20.53 12.9 20.18 13.18L17.89 14.5L15.39 12L17.89 9.5L20.16 10.81Z" fill="#ff3e00"/>
								<path d="M6.05 2.66L16.81 8.88L14.54 11.15L6.05 2.66Z" fill="#00e081" opacity="0.9"/>
							</svg>
							{t("footer.browseAllApps")}
						</a>
					</div>
				</div>
			)}

			<!-- Main footer body (light/dark responsive) -->
			<div class="footer-body">
				<!-- Subtle top separator -->
				<div class="h-px w-full bg-gray-200 dark:bg-gray-800/60"></div>

				<div class="relative mx-auto max-w-7xl px-4 pb-0 pt-14 sm:px-6 lg:px-8">
					<div class="grid gap-12 sm:grid-cols-2 lg:grid-cols-[2.2fr_1fr_1fr_1.3fr]">

						<!-- Column 1: Brand -->
						<div class="space-y-6">
							<a href={navHome} class="inline-flex items-center gap-3 group">
								<img
									src="/logo.png"
									alt="FusionCore Apps"
									class="h-10 w-10 rounded-xl object-contain transition-transform duration-300 group-hover:scale-105"
								/>
								<span class="font-bold text-gray-900 dark:text-white" style="font-family: var(--font-display); font-size: 16px; letter-spacing: -0.025em;">
									FusionCore Apps
								</span>
							</a>

							<p class="max-w-[240px] text-sm leading-relaxed text-gray-500 dark:text-gray-500">
								{t("footer.tagline")}
							</p>

							<!-- Mini stat strip -->
							<div class="flex items-center gap-4">
								<div>
									<div class="text-base font-bold text-gray-900 dark:text-white" style="font-family: var(--font-display); letter-spacing: -0.03em;">3.3K+</div>
									<div class="text-[10px] font-semibold uppercase tracking-wider text-gray-400 dark:text-gray-600">{t("footer.downloads")}</div>
								</div>
								<div class="h-7 w-px bg-gray-200 dark:bg-gray-800"></div>
								<div>
									<div class="text-base font-bold text-gray-900 dark:text-white" style="font-family: var(--font-display); letter-spacing: -0.03em;">4.5★</div>
									<div class="text-[10px] font-semibold uppercase tracking-wider text-gray-400 dark:text-gray-600">{t("footer.rating")}</div>
								</div>
								<div class="h-7 w-px bg-gray-200 dark:bg-gray-800"></div>
								<div>
									<div class="text-base font-bold text-gray-900 dark:text-white" style="font-family: var(--font-display); letter-spacing: -0.03em;">{t("footer.free")}</div>
									<div class="text-[10px] font-semibold uppercase tracking-wider text-gray-400 dark:text-gray-600">{t("footer.always")}</div>
								</div>
							</div>

							<!-- Google Play badge -->
							<a
								href="https://play.google.com/store/apps/dev?id=5173536626513659112"
								target="_blank"
								rel="noopener noreferrer"
								aria-label={t("footer.availableOn")}
								class="inline-flex items-center gap-2 rounded-lg border border-gray-200 px-4 py-2 text-sm font-medium text-gray-600 transition-all duration-200 hover:border-gray-400 hover:text-gray-900 dark:border-gray-800 dark:text-gray-400 dark:hover:border-gray-600 dark:hover:text-white"
							>
								<GooglePlayLogo class="h-4 w-4" />
								<span>Google Play</span>
							</a>
						</div>

						<!-- Column 2: Navigation -->
						<div>
							<h3 class="footer__col-title">{t("footer.navigation")}</h3>
							<ul class="space-y-0.5">
								<li><a class="footer__link" href={navHome}>{t("nav.home")}</a></li>
								<li><a class="footer__link" href={navApps}>{t("nav.apps")}</a></li>
								<li><a class="footer__link" href={navBlog}>{t("nav.blog")}</a></li>
								<li><a class="footer__link" href={getLocalePath("/compare-apps", currentLocale as Locale)}>{t("footer.compareApps")}</a></li>
								<li><a class="footer__link" href={navContact}>{t("nav.contact")}</a></li>
								<li><a class="footer__link" href={navPrivacy}>{t("nav.privacy")}</a></li>
								<li><a class="footer__link" href={navTerms}>{t("nav.terms")}</a></li>
							</ul>
						</div>

						<!-- Column 3: Our Apps -->
						<div>
							<h3 class="footer__col-title">{t("footer.ourApps")}</h3>
							<ul class="space-y-0.5">
								<li><a class="footer__link" href={getLocalePath("/apps/bible-tpt", currentLocale as Locale)}>Bible TPT</a></li>
								<li><a class="footer__link" href={getLocalePath("/apps/biblia-tla", currentLocale as Locale)}>Biblia TLA</a></li>
								<li><a class="footer__link" href={getLocalePath("/cartwise", currentLocale as Locale)}>CartWise</a></li>
								<li>
									<a class="footer__link" href={getLocalePath("/apps/claimly-receipt-tracker", currentLocale as Locale)}>
										Claimly
										<span class="ml-1 inline-flex items-center rounded-full px-1.5 py-0.5 text-[10px] font-bold" style="background: rgba(234,88,12,0.10); color: #ea580c;">Beta</span>
									</a>
								</li>
								<li>
									<a class="footer__link" href={getLocalePath("/apps/expiroo", currentLocale as Locale)}>
										Expiroo
										<span class="ml-1 inline-flex items-center rounded-full px-1.5 py-0.5 text-[10px] font-bold" style="background: rgba(234,88,12,0.10); color: #ea580c;">Beta</span>
									</a>
								</li>
								<li class="pt-2">
									<a class="footer__link footer__link--accent" href={navApps}>
										{t("footer.viewAll")}
									</a>
								</li>
							</ul>
						</div>

						<!-- Column 4: Connect -->
						<div>
							<h3 class="footer__col-title">{t("footer.getInTouch")}</h3>
							<ul class="space-y-1">
								<li>
									<a
										class="footer__link"
										href="https://play.google.com/store/apps/dev?id=5173536626513659112"
										target="_blank"
										rel="noopener noreferrer"
									>
										<GooglePlayLogo class="h-3.5 w-3.5 flex-shrink-0 opacity-60" />
										<span>Google Play Store</span>
									</a>
								</li>
								<li>
									<a class="footer__link" href="mailto:jeanrey.ese@gmail.com">
										<svg class="h-3.5 w-3.5 flex-shrink-0 opacity-60" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
											<rect width="20" height="16" x="2" y="4" rx="2"/>
											<path d="m22 7-8.97 5.7a1.94 1.94 0 0 1-2.06 0L2 7"/>
										</svg>
										<span>jeanrey.ese@gmail.com</span>
									</a>
								</li>
								<li>
									<a
										class="footer__link"
										href="https://github.com/Z3r0J"
										target="_blank"
										rel="noopener noreferrer"
									>
										<svg class="h-3.5 w-3.5 flex-shrink-0 opacity-60" viewBox="0 0 24 24" fill="currentColor">
											<path d="M12 0C5.37 0 0 5.37 0 12c0 5.31 3.435 9.795 8.205 11.385.6.105.825-.255.825-.57 0-.285-.015-1.23-.015-2.235-3.015.555-3.795-.735-4.035-1.41-.135-.345-.72-1.41-1.23-1.695-.42-.225-1.02-.78-.015-.795.945-.015 1.62.87 1.845 1.23 1.08 1.815 2.805 1.305 3.495.99.105-.78.42-1.305.765-1.605-2.67-.3-5.46-1.335-5.46-5.925 0-1.305.465-2.385 1.23-3.225-.12-.3-.54-1.53.12-3.18 0 0 1.005-.315 3.3 1.23.96-.27 1.98-.405 3-.405s2.04.135 3 .405c2.295-1.56 3.3-1.23 3.3-1.23.66 1.65.24 2.88.12 3.18.765.84 1.23 1.905 1.23 3.225 0 4.605-2.805 5.625-5.475 5.925.435.375.81 1.095.81 2.22 0 1.605-.015 2.895-.015 3.3 0 .315.225.69.825.57A12.02 12.02 0 0 0 24 12c0-6.63-5.37-12-12-12z"/>
										</svg>
										<span>GitHub / Z3r0J</span>
									</a>
								</li>
							</ul>
						</div>
					</div>

					<!-- Bottom bar -->
					<div class="relative z-10 mt-14 flex flex-col items-center justify-between gap-3 border-t border-gray-200 py-6 dark:border-gray-800/60 sm:flex-row">
						<p class="text-sm text-gray-400 dark:text-gray-600">
							&copy; {year} FusionCore Apps. {t("footer.allRightsReserved")}
						</p>
						<div class="flex items-center gap-5 text-sm">
							<a class="text-gray-400 transition-colors hover:text-gray-700 dark:text-gray-600 dark:hover:text-gray-400" href={navPrivacy}>{t("footer.privacyPolicy")}</a>
							<span class="text-gray-200 dark:text-gray-800">&middot;</span>
							<a class="text-gray-400 transition-colors hover:text-gray-700 dark:text-gray-600 dark:hover:text-gray-400" href={navTerms}>{t("footer.termsOfService")}</a>
						</div>
						<p class="text-sm text-gray-400 dark:text-gray-600">
							{t("footer.builtForAndroid")}
						</p>
					</div>
				</div>

				<!-- Background watermark -->
				<div class="footer__watermark" aria-hidden="true">
					<span>FusionCore Apps</span>
				</div>
			</div>
		</footer>

		<!-- Navigation scripts -->
		<script is:inline>
			function setupNav() {
				const header   = document.getElementById("site-header");
				const menuBtn  = document.getElementById("menu-btn");
				const overlay  = document.getElementById("mobile-overlay");
				const closeBtn = document.getElementById("close-overlay");

				if (!header || !menuBtn || !overlay || !closeBtn) return;
				if (menuBtn.dataset.navReady) return; // prevent double-bind
				menuBtn.dataset.navReady = "true";

				// ── Scroll: morph pill ──
				let ticking = false;
				function onScroll() {
					if (!ticking) {
						requestAnimationFrame(function () {
							if (window.scrollY > 24) {
								header.dataset.scrolled = "";
							} else {
								delete header.dataset.scrolled;
							}
							ticking = false;
						});
						ticking = true;
					}
				}
				window.addEventListener("scroll", onScroll, { passive: true });
				onScroll(); // run immediately on page load

				// ── Mobile overlay ──
				function openMenu() {
					overlay.classList.add("is-open");
					overlay.setAttribute("aria-hidden", "false");
					menuBtn.setAttribute("aria-expanded", "true");
					document.body.style.overflow = "hidden";
				}

				function closeMenu() {
					overlay.classList.remove("is-open");
					overlay.setAttribute("aria-hidden", "true");
					menuBtn.setAttribute("aria-expanded", "false");
					document.body.style.overflow = "";
				}

				menuBtn.addEventListener("click", function () {
					overlay.classList.contains("is-open") ? closeMenu() : openMenu();
				});

				closeBtn.addEventListener("click", closeMenu);

				// Close on overlay link clicks
				overlay.querySelectorAll("a").forEach(function (link) {
					link.addEventListener("click", closeMenu);
				});

				// Close on Escape
				document.addEventListener("keydown", function (e) {
					if (e.key === "Escape" && overlay.classList.contains("is-open")) closeMenu();
				});
			}

			if (document.readyState === "loading") {
				document.addEventListener("DOMContentLoaded", setupNav);
			} else {
				setupNav();
			}

			document.addEventListener("astro:page-load", setupNav);
		</script>
	</body>
</html>
